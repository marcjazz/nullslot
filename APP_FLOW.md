# 🧬 FULL END-TO-END LIFECYCLE

## University Timetable System

*(No student login, coordinator-driven, offline-friendly)*

---

## 🟢 PHASE 0: System Bootstrap (One-time)

```
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│ Sysadmin     │      │ Rust Backend │      │ Database     │
│ (Installer)  │      │              │      │              │
└──────┬───────┘      └──────┬───────┘      └──────┬───────┘
       │                     │                     │
       │ Start system        │                     │
       │ BOOTSTRAP=true      │                     │
       │                     │                     │
       │ Create Admin        │                     │
       │ (email/password)   │                     │
       ├────────────────────▶│                     │
       │                     │ Insert user         │
       │                     │ role=ADMIN          │
       │                     ├────────────────────▶│
       │                     │                     │
       │                     │ Disable bootstrap   │
       │◀────────────────────┤                     │
```

🔒 Bootstrap endpoint self-destructs after first admin.

---

## 🔐 PHASE 1: Authentication (Teachers, Coordinators, Admins)

### 1A. Login with University SSO (OIDC)

```
┌─────────┐      ┌────────────┐      ┌──────────────┐      ┌────────────┐
│ User    │      │ Frontend   │      │ Univ SSO     │      │ Backend    │
│ (Admin/ │      │ React SPA  │      │ (OIDC)       │      │ Rust       │
│ Coord/  │      └────┬───────┘      └────┬─────────┘      └────┬───────┘
│ Teacher)│           │                   │                    │
└────┬────┘           │                   │                    │
     │ Login           │                   │                    │
     ├────────────────▶│ Redirect to SSO   │                    │
     │                 ├──────────────────▶│                    │
     │                 │ Authenticate       │                    │
     │                 │◀──────────────────┤                    │
     │                 │ Auth code          │                    │
     │                 ├────────────────────────────────────────▶│
     │                 │                   │ Verify identity     │
     │                 │                   │ Lookup local user   │
     │                 │                   │ Resolve role       │
     │                 │                   │ Issue JWT + refresh│
     │◀────────────────┴───────────────────┴────────────────────┘
```

Identity comes from SSO.
**Authority comes from your database.**

---

### 1B. Login without SSO (Magic Link)

```
┌─────────┐      ┌────────────┐      ┌──────────────┐
│ User    │      │ Frontend   │      │ Rust Backend │
│         │      │ React SPA  │      │              │
└────┬────┘      └────┬───────┘      └────┬─────────┘
     │ Enter email     │                   │
     ├────────────────▶│                   │
     │                 │ POST /auth/magic  │
     │                 ├──────────────────▶│
     │                 │                   │ Send email link
     │ Open link       │                   │
     ├────────────────────────────────────▶│
     │                 │                   │ Validate token
     │                 │                   │ Issue JWT
     │◀────────────────┴───────────────────┘
```

---

## 🧑‍🏫 PHASE 2: Availability Collection (Draft State)

```
┌─────────┐      ┌────────────┐      ┌──────────────┐
│ Teacher │      │ Frontend   │      │ Rust Backend │
│         │      │ React SPA  │      │              │
└────┬────┘      └────┬───────┘      └────┬─────────┘
     │ Enter availability                 │
     ├────────────────▶│                 │
     │                 │ POST /availability
     │                 ├────────────────▶│
     │                 │                 │ Validate role=TEACHER
     │                 │                 │ Save DRAFT availability
     │◀────────────────┴─────────────────┘
```

Teachers **never** create classes.
They only describe when they are available.

---

## 🧠 PHASE 3: Conflict Resolution (Coordinator Authority)

```
┌────────────┐      ┌────────────┐      ┌──────────────┐
│ Coordinator│      │ Frontend   │      │ Rust Backend │
│            │      │ React SPA  │      │              │
└────┬───────┘      └────┬───────┘      └────┬─────────┘
     │ View conflicts    │                   │
     ├──────────────────▶│                   │
     │                   │ GET /conflicts    │
     │                   ├──────────────────▶│
     │                   │                   │ Compute overlaps
     │                   │                   │ Suggest resolutions
     │◀──────────────────┤                   │
     │                   │                   │
     │ Assign classes    │                   │
     ├──────────────────▶│                   │
     │                   │ POST /assignments│
     │                   ├──────────────────▶│
     │                   │                   │ Save DRAFT timetable
     │◀──────────────────┴───────────────────┘
```

Coordinator decisions are authoritative and logged.

---

## 📣 PHASE 4: Publish Timetable (Hard Boundary)

```
┌────────────┐      ┌────────────┐      ┌──────────────┐
│ Coordinator│      │ Frontend   │      │ Rust Backend │
│            │      │ React SPA  │      │              │
└────┬───────┘      └────┬───────┘      └────┬─────────┘
     │ Publish           │                   │
     ├──────────────────▶│                   │
     │                   │ POST /publish     │
     │                   ├──────────────────▶│
     │                   │                   │ Freeze timetable
     │                   │                   │ Generate ICS feeds
     │                   │                   │ Notify teachers
     │◀──────────────────┴───────────────────┘
```

After this:

* Drafts are immutable
* Teachers receive calendar updates
* Students gain read-only access

---

## 👩‍🎓 PHASE 5: Student Access (No Login)

```
┌─────────┐      ┌────────────┐      ┌──────────────┐
│ Student │      │ Frontend   │      │ Rust Backend │
│         │      │ React SPA  │      │              │
└────┬────┘      └────┬───────┘      └────┬─────────┘
     │ Open timetable  │                   │
     ├────────────────▶│                   │
     │                 │ GET /timetable    │
     │                 ├──────────────────▶│
     │                 │                   │ Serve PUBLISHED only
     │◀────────────────┴───────────────────┘
```

* Filter by course, year, room
* IndexedDB caches for offline read-only use
* No auth, no cookies, no risk

---

## 📅 PHASE 6: Calendar Consumption (Teachers)

```
┌──────────────┐      ┌──────────────┐
│ Calendar App │      │ Rust Backend │
│              │      │              │
└──────┬───────┘      └──────┬───────┘
       │ GET /cal/{token}.ics │
       ├────────────────────▶│
       │                     │ Validate token
       │                     │ Generate ICS
       │◀────────────────────┤
```

Survives outages.
Survives offline.
Survives forgetful humans.
